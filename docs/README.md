# RGVL Platform Documentation

- See `data/` and `web/` folders for service-specific docs
- See `ARCHITECTURE.md` and `INTEGRATION_PLAN.md` in `data/` for backend details
- See `README.md` in `web/` for dashboard usage

## Monorepo Layout
- All platform code and docs are now under `rgvl/`
- Use the root README for quick start and structure
